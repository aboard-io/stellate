// dx7_alg31 — generated on demand for the state.dx7 contract (see dx7_alg5.dsp:
// per-algorithm builds because the runtime 32-algo switch OOMs libfaust-wasm).
declare name "dx7_alg31";
import("stdfaust.lib");
process = dx.algorithm(31);
